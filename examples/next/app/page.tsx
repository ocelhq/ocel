export default function Home() {
  return (
    <main>
      <h1>Ocel Todos</h1>
      <p>
        A todos CRUD API backed by <code>postgres(&quot;main&quot;)</code>. See{" "}
        <code>/api/todos</code>.
      </p>
      <p>
        The <a href="/cache">cache example</a> shows Cache Components and tag
        revalidation.
      </p>
    </main>
  );
}
