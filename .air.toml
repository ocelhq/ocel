root = "."
tmp_dir = "tmp"

[build]
cmd = "node scripts/snapshot.mjs"
entrypoint = ["true"]
include_ext = ["go"]
exclude_dir = ["tmp"]

[build.windows]
entrypoint = ["cmd", "/c", "exit 0"]

[log]
main_only = true
