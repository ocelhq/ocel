# Claude subscription token — the agent uses it to authenticate inside the sandbox.
# Generate with `claude setup-token` on your host and paste the result here.
CLAUDE_CODE_OAUTH_TOKEN=

# To use an Anthropic API key instead of a subscription token, uncomment:
# ANTHROPIC_API_KEY=
