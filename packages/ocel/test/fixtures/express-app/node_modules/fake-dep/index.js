import { tag } from "./helper";

// Mirrors the ocel-dist failure class: a published ESM package whose own dist
// uses extensionless relative imports (only runnable under a bundler). The
// builder must rewrite `./helper` -> `./helper.js` in this COPIED dep file.
export function stamp(value) {
  return tag(value);
}
