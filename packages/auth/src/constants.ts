// Client identifier used by the Ocel Go CLI when requesting device
// authorization. Kept as a single named constant so the server-side
// validation and any docs/UI referencing it stay in sync.
export const OCEL_CLI_CLIENT_ID = "ocel-cli";
