---
"ocel": patch
---

Deliver a plaintext-class variable to a deployed function under its own name, so
a library that reads the process environment itself finds it. Resolved values
are exported into the build environment before the app build, and a function
environment that exceeds the platform's limit fails the deploy with a per-key
byte breakdown naming reclassification as the remedy.
