// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: runtime/v1/runtime.proto

package runtimev1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/ocelhq/ocel/pkg/proto/runtime/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// RuntimeServiceName is the fully-qualified name of the RuntimeService service.
	RuntimeServiceName = "runtime.v1.RuntimeService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// RuntimeServicePresignUploadProcedure is the fully-qualified name of the RuntimeService's
	// PresignUpload RPC.
	RuntimeServicePresignUploadProcedure = "/runtime.v1.RuntimeService/PresignUpload"
	// RuntimeServiceVerifyUploadSignatureProcedure is the fully-qualified name of the RuntimeService's
	// VerifyUploadSignature RPC.
	RuntimeServiceVerifyUploadSignatureProcedure = "/runtime.v1.RuntimeService/VerifyUploadSignature"
	// RuntimeServiceGetUploadStatusProcedure is the fully-qualified name of the RuntimeService's
	// GetUploadStatus RPC.
	RuntimeServiceGetUploadStatusProcedure = "/runtime.v1.RuntimeService/GetUploadStatus"
)

// RuntimeServiceClient is a client for the runtime.v1.RuntimeService service.
type RuntimeServiceClient interface {
	// PresignUpload writes the pending session and mints the presigned PUT
	// targets. The response's targets are index-aligned with the request's
	// files.
	PresignUpload(context.Context, *v1.PresignUploadRequest) (*v1.PresignUploadResponse, error)
	// VerifyUploadSignature re-derives the per-session HMAC over the signed
	// payload and constant-time compares it. It is read-only: on a valid
	// signature it returns the stored metadata so the route need never hold the
	// session secret. The secret never leaves the store.
	VerifyUploadSignature(context.Context, *v1.VerifyUploadSignatureRequest) (*v1.VerifyUploadSignatureResponse, error)
	// GetUploadStatus reads the shared store and reports a session's state. It
	// is read-only and drives op=poll; reading the shared store keeps it
	// correct across app instances.
	GetUploadStatus(context.Context, *v1.GetUploadStatusRequest) (*v1.GetUploadStatusResponse, error)
}

// NewRuntimeServiceClient constructs a client for the runtime.v1.RuntimeService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewRuntimeServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) RuntimeServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	runtimeServiceMethods := v1.File_runtime_v1_runtime_proto.Services().ByName("RuntimeService").Methods()
	return &runtimeServiceClient{
		presignUpload: connect.NewClient[v1.PresignUploadRequest, v1.PresignUploadResponse](
			httpClient,
			baseURL+RuntimeServicePresignUploadProcedure,
			connect.WithSchema(runtimeServiceMethods.ByName("PresignUpload")),
			connect.WithClientOptions(opts...),
		),
		verifyUploadSignature: connect.NewClient[v1.VerifyUploadSignatureRequest, v1.VerifyUploadSignatureResponse](
			httpClient,
			baseURL+RuntimeServiceVerifyUploadSignatureProcedure,
			connect.WithSchema(runtimeServiceMethods.ByName("VerifyUploadSignature")),
			connect.WithClientOptions(opts...),
		),
		getUploadStatus: connect.NewClient[v1.GetUploadStatusRequest, v1.GetUploadStatusResponse](
			httpClient,
			baseURL+RuntimeServiceGetUploadStatusProcedure,
			connect.WithSchema(runtimeServiceMethods.ByName("GetUploadStatus")),
			connect.WithClientOptions(opts...),
		),
	}
}

// runtimeServiceClient implements RuntimeServiceClient.
type runtimeServiceClient struct {
	presignUpload         *connect.Client[v1.PresignUploadRequest, v1.PresignUploadResponse]
	verifyUploadSignature *connect.Client[v1.VerifyUploadSignatureRequest, v1.VerifyUploadSignatureResponse]
	getUploadStatus       *connect.Client[v1.GetUploadStatusRequest, v1.GetUploadStatusResponse]
}

// PresignUpload calls runtime.v1.RuntimeService.PresignUpload.
func (c *runtimeServiceClient) PresignUpload(ctx context.Context, req *v1.PresignUploadRequest) (*v1.PresignUploadResponse, error) {
	response, err := c.presignUpload.CallUnary(ctx, connect.NewRequest(req))
	if response != nil {
		return response.Msg, err
	}
	return nil, err
}

// VerifyUploadSignature calls runtime.v1.RuntimeService.VerifyUploadSignature.
func (c *runtimeServiceClient) VerifyUploadSignature(ctx context.Context, req *v1.VerifyUploadSignatureRequest) (*v1.VerifyUploadSignatureResponse, error) {
	response, err := c.verifyUploadSignature.CallUnary(ctx, connect.NewRequest(req))
	if response != nil {
		return response.Msg, err
	}
	return nil, err
}

// GetUploadStatus calls runtime.v1.RuntimeService.GetUploadStatus.
func (c *runtimeServiceClient) GetUploadStatus(ctx context.Context, req *v1.GetUploadStatusRequest) (*v1.GetUploadStatusResponse, error) {
	response, err := c.getUploadStatus.CallUnary(ctx, connect.NewRequest(req))
	if response != nil {
		return response.Msg, err
	}
	return nil, err
}

// RuntimeServiceHandler is an implementation of the runtime.v1.RuntimeService service.
type RuntimeServiceHandler interface {
	// PresignUpload writes the pending session and mints the presigned PUT
	// targets. The response's targets are index-aligned with the request's
	// files.
	PresignUpload(context.Context, *v1.PresignUploadRequest) (*v1.PresignUploadResponse, error)
	// VerifyUploadSignature re-derives the per-session HMAC over the signed
	// payload and constant-time compares it. It is read-only: on a valid
	// signature it returns the stored metadata so the route need never hold the
	// session secret. The secret never leaves the store.
	VerifyUploadSignature(context.Context, *v1.VerifyUploadSignatureRequest) (*v1.VerifyUploadSignatureResponse, error)
	// GetUploadStatus reads the shared store and reports a session's state. It
	// is read-only and drives op=poll; reading the shared store keeps it
	// correct across app instances.
	GetUploadStatus(context.Context, *v1.GetUploadStatusRequest) (*v1.GetUploadStatusResponse, error)
}

// NewRuntimeServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewRuntimeServiceHandler(svc RuntimeServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	runtimeServiceMethods := v1.File_runtime_v1_runtime_proto.Services().ByName("RuntimeService").Methods()
	runtimeServicePresignUploadHandler := connect.NewUnaryHandlerSimple(
		RuntimeServicePresignUploadProcedure,
		svc.PresignUpload,
		connect.WithSchema(runtimeServiceMethods.ByName("PresignUpload")),
		connect.WithHandlerOptions(opts...),
	)
	runtimeServiceVerifyUploadSignatureHandler := connect.NewUnaryHandlerSimple(
		RuntimeServiceVerifyUploadSignatureProcedure,
		svc.VerifyUploadSignature,
		connect.WithSchema(runtimeServiceMethods.ByName("VerifyUploadSignature")),
		connect.WithHandlerOptions(opts...),
	)
	runtimeServiceGetUploadStatusHandler := connect.NewUnaryHandlerSimple(
		RuntimeServiceGetUploadStatusProcedure,
		svc.GetUploadStatus,
		connect.WithSchema(runtimeServiceMethods.ByName("GetUploadStatus")),
		connect.WithHandlerOptions(opts...),
	)
	return "/runtime.v1.RuntimeService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case RuntimeServicePresignUploadProcedure:
			runtimeServicePresignUploadHandler.ServeHTTP(w, r)
		case RuntimeServiceVerifyUploadSignatureProcedure:
			runtimeServiceVerifyUploadSignatureHandler.ServeHTTP(w, r)
		case RuntimeServiceGetUploadStatusProcedure:
			runtimeServiceGetUploadStatusHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedRuntimeServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedRuntimeServiceHandler struct{}

func (UnimplementedRuntimeServiceHandler) PresignUpload(context.Context, *v1.PresignUploadRequest) (*v1.PresignUploadResponse, error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("runtime.v1.RuntimeService.PresignUpload is not implemented"))
}

func (UnimplementedRuntimeServiceHandler) VerifyUploadSignature(context.Context, *v1.VerifyUploadSignatureRequest) (*v1.VerifyUploadSignatureResponse, error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("runtime.v1.RuntimeService.VerifyUploadSignature is not implemented"))
}

func (UnimplementedRuntimeServiceHandler) GetUploadStatus(context.Context, *v1.GetUploadStatusRequest) (*v1.GetUploadStatusResponse, error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("runtime.v1.RuntimeService.GetUploadStatus is not implemented"))
}
