// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: provider/cost/v1/cost.proto

package costv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/ocelhq/ocel/pkg/proto/provider/cost/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// CostServiceName is the fully-qualified name of the CostService service.
	CostServiceName = "provider.cost.v1.CostService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// CostServicePriceProcedure is the fully-qualified name of the CostService's Price RPC.
	CostServicePriceProcedure = "/provider.cost.v1.CostService/Price"
)

// CostServiceClient is a client for the provider.cost.v1.CostService service.
type CostServiceClient interface {
	Price(context.Context, *v1.PriceRequest) (*v1.Estimate, error)
}

// NewCostServiceClient constructs a client for the provider.cost.v1.CostService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewCostServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) CostServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	costServiceMethods := v1.File_provider_cost_v1_cost_proto.Services().ByName("CostService").Methods()
	return &costServiceClient{
		price: connect.NewClient[v1.PriceRequest, v1.Estimate](
			httpClient,
			baseURL+CostServicePriceProcedure,
			connect.WithSchema(costServiceMethods.ByName("Price")),
			connect.WithClientOptions(opts...),
		),
	}
}

// costServiceClient implements CostServiceClient.
type costServiceClient struct {
	price *connect.Client[v1.PriceRequest, v1.Estimate]
}

// Price calls provider.cost.v1.CostService.Price.
func (c *costServiceClient) Price(ctx context.Context, req *v1.PriceRequest) (*v1.Estimate, error) {
	response, err := c.price.CallUnary(ctx, connect.NewRequest(req))
	if response != nil {
		return response.Msg, err
	}
	return nil, err
}

// CostServiceHandler is an implementation of the provider.cost.v1.CostService service.
type CostServiceHandler interface {
	Price(context.Context, *v1.PriceRequest) (*v1.Estimate, error)
}

// NewCostServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewCostServiceHandler(svc CostServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	costServiceMethods := v1.File_provider_cost_v1_cost_proto.Services().ByName("CostService").Methods()
	costServicePriceHandler := connect.NewUnaryHandlerSimple(
		CostServicePriceProcedure,
		svc.Price,
		connect.WithSchema(costServiceMethods.ByName("Price")),
		connect.WithHandlerOptions(opts...),
	)
	return "/provider.cost.v1.CostService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case CostServicePriceProcedure:
			costServicePriceHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedCostServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedCostServiceHandler struct{}

func (UnimplementedCostServiceHandler) Price(context.Context, *v1.PriceRequest) (*v1.Estimate, error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("provider.cost.v1.CostService.Price is not implemented"))
}
