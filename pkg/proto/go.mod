// Lean shared module: generated protobuf/connect bindings consumed by the CLI
// (github.com/ocelhq/ocel), the Go SDK (github.com/ocelhq/ocel/sdk), and the
// cloud provider binaries (github.com/ocelhq/ocel/cloud/*). It deliberately
// carries only protobuf/connect so importers don't inherit each other's deps.
module github.com/ocelhq/ocel/pkg/proto

go 1.25.11

require (
	connectrpc.com/connect v1.20.0
	google.golang.org/protobuf v1.36.11
)
