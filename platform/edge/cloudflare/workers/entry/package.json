{
  "name": "@platform/cf-entry",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir=dist",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "fixtures:image": "node scripts/generate-image-fixtures.mjs",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.4",
    "@types/node": "^26.1.1",
    "typescript": "^5.5.2",
    "vitest": "~3.2.0",
    "wrangler": "^4.110.0"
  },
  "dependencies": {
    "@framework/next-cache": "workspace:*",
    "@next/routing": "^16.2.10",
    "@platform/edge-contract": "workspace:*",
    "aws4fetch": "^1.0.20"
  }
}
