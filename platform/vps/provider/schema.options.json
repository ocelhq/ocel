{
  "additionalProperties": false,
  "properties": {
    "name": {
      "const": "vps"
    },
    "options": {
      "additionalProperties": false,
      "properties": {
        "bucket": {
          "additionalProperties": false,
          "description": "An S3-compatible store to keep this project's buckets in, instead of the one a box runs for itself. Each declared bucket becomes a key prefix inside the named bucket.",
          "properties": {
            "accessKeyId": {
              "description": "The public half of the credential that reaches the store.",
              "type": "string"
            },
            "bucket": {
              "description": "The bucket every declared bucket takes a key prefix inside.",
              "type": "string"
            },
            "endpoint": {
              "description": "The address the store answers on.",
              "type": "string"
            },
            "pathStyle": {
              "description": "Address buckets as a path segment rather than a subdomain. Stores that serve no wildcard DNS need it.",
              "type": "boolean"
            },
            "region": {
              "description": "The region the store signs requests against.",
              "type": "string"
            },
            "secretAccessKey": {
              "description": "The secret half of the credential that reaches the store.",
              "type": "string"
            }
          },
          "required": [
            "endpoint",
            "region",
            "bucket",
            "accessKeyId",
            "secretAccessKey"
          ],
          "title": "ExternalStore",
          "type": "object"
        },
        "certificates": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Certificates to serve a hostname with, keyed by hostname, valued by the path to the certificate on the machine.",
          "type": "object"
        },
        "deployKey": {
          "description": "The public key the ocel-deploy login is to answer to, as a path from / or from ~/. Omit it and bootstrap mirrors the keys the login it bootstraps with already answers to.",
          "type": "string"
        },
        "ssh": {
          "description": "The machine to deploy onto: a Host alias from ssh_config, or the destination spelled out.",
          "oneOf": [
            {
              "type": "string"
            },
            {
              "additionalProperties": false,
              "properties": {
                "host": {
                  "description": "The hostname or address to reach the machine at.",
                  "type": "string"
                },
                "identityFile": {
                  "description": "The private key to authenticate with, as a path.",
                  "type": "string"
                },
                "port": {
                  "description": "The port sshd listens on. Omit it and ssh's own default stands.",
                  "type": "integer"
                },
                "user": {
                  "description": "The account to log in as. Omit it and ssh resolves the user itself.",
                  "type": "string"
                }
              },
              "required": [
                "host"
              ],
              "title": "Target",
              "type": "object"
            }
          ]
        }
      },
      "required": [
        "ssh"
      ],
      "title": "VpsProviderOptions",
      "type": "object"
    }
  },
  "required": [
    "name"
  ],
  "type": "object"
}
