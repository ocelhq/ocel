{
  "$schema": "https://ocel.dev/schema/0.0.0/ocel.schema.json",
  "slug": "workspace",
  "provider": {
    "gcp": { "project": "${OCEL_GCP_PROJECT}", "region": "${OCEL_GCP_REGION}" }
  },
  "apps": [
    { "name": "next", "path": "./apps/next", "folder": "/next" },
    { "name": "express", "path": "./apps/express", "folder": "/express" }
  ]
}
